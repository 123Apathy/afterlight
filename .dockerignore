node_modules
server/node_modules
.git
dist
.expo
# never bake env into the image — Cloud Run injects vars; .env would also
# hardcode EXPO_PUBLIC_API_BASE=localhost into the web build.
.env
.env.*
server/.env
# preview-only real photos (already gitignored)
assets/demo
*.log
.impeccable
.verify-shots
